package PrEis.gui;

/** 
 * Empty stub 'wrapper' implemented by all {@link UIObject} callback types. Used
 * as a variable within {@link UIObjectPropPayload} s.t. all callback types can
 * share it; versus manually specifying variables for each, subclassing a family
 * of prop payload objects, etc.
 */
public interface IGUICallBack {
  /* STUB */
}
